# NOTE: floors, not hard pins — lock exact versions after a clean install/test.
# The original code pinned a deprecated 2024-era stack; this targets current.

# Core orchestration
langchain-core>=0.3
langchain>=0.3
langgraph>=0.2.40
langchain-community>=0.3
PyYAML>=6.0

# LLM providers — install only what LLM_PROVIDER needs.
# openai-compatible path (ChatOpenAI) covers OpenRouter AND z.ai-direct (GLM-5.2).
langchain-openai>=0.2
# langchain-groq>=0.2
# langchain-google-genai>=2.0
# langchain-anthropic>=0.2

# RAG: vector store, embeddings, cross-encoder reranker, BM25, splitting, PDFs
langchain-chroma>=0.1.4
langchain-huggingface>=0.1
langchain-text-splitters>=0.3
sentence-transformers>=3.0
rank-bm25>=0.2.2
pypdf>=5.0

# Tools / UI / utilities
tavily-python>=0.5
streamlit>=1.38
requests>=2.32
python-dotenv>=1.0
pydantic>=2.7
